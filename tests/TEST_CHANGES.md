# Test changes during the pydantic v1 → v2 migration

Rule (design spec §5.1): a test may be edited only when it uses a pydantic **v1 API
form** in test-local code — never to accommodate a behaviour change. Every edit is
listed here.

| Subtask | File | Change | Why (v1 form replaced) |
|---|---|---|---|
| S0 | `tests/conftest.py` | xdist grouping hook | new infrastructure, no behaviour |
| S0 | `test_receiver_zmq.py`, `test_tracker_zmq.py` | `xdist_group("zmq")` marker | new infrastructure, no behaviour |
| S1-T5 | `test_pydantic_types.py` | `import pydantic.v1 as pyd` → `import pydantic as pyd` | v1 shim import |
| S1-T6 | `test_pydantic_types.py` (`TestNumpyType`, `TestYamlInput`) | `pyd.parse_raw_as(plt.X, x.json())` → `plt.X.model_validate_json(x.model_dump_json())`; `pyd.parse_obj_as(plt.X, x.dict()["__root__"])` → `plt.X.model_validate(x.dict()["__root__"])` | `parse_raw_as`/`parse_obj_as`/`.json()` are v1-only APIs |
| S1-T6 | `test_pydantic_types.py` (`TestItemType`, `TestCallableDef`) | same `parse_raw_as`/`parse_obj_as` → `model_validate_json`/`model_validate` replacement, applied ahead of the T7 class conversion | `parse_raw_as`/`parse_obj_as`/`.json()` are v1-only APIs |
| S1-T7 | `test_pydantic_types.py` | added `import typing as t` and `TestCallableDef.test_string_annotations_resolved` (new regression test, not a v1-form edit) | forward-ref annotation resolution added to `CallableDef.args_type`/`return_type` |
| S1 | `tests/pipelime/utils/test_pydantic_compat.py` | the `PydanticUserError` pin moved from `Field("x", regex="x")` to `Field("x", const=True)`; new `regex=`/`pattern=` test | `regex=` is now translated to v2 `pattern=` by the pipelime `Field` wrapper, so it no longer raises |
| S2b-T8 | `tests/pipelime/choixe/visitors/test_decoder.py`, `test_processor.py` | `from pydantic.v1 import BaseModel` → `from pydantic import BaseModel` | v1 shim import (`$model`/`decode` now require v2 models) |
| S2b-T8 | `tests/pipelime/choixe/visitors/test_processor.py::TestProcessor::test_model` | `assert process(...) == expected` → compare `type(m).__name__` and `model_dump()` of result and expected | the `$model` symbol is imported from the test *file* (a second module object), so the classes differ; the equality relied on v1 `BaseModel.__eq__` comparing `.dict()` across classes (v2 requires the same class) |
| S2b-T9 | `tests/sample_data/cli/extra_commands.py` | `from pydantic.v1 import Field` → `from pipelime.piper import Field` | v1 shim import; the fields carry `piper_port=` (migration-guide edit) |
| S2b-T9 | `tests/sample_data/cli/extra_operators.py` | `from pydantic.v1 import Field` → `from pydantic import Field` | v1 shim import (no piper flag) |
| S2b-T9 | `tests/sample_data/cli/ckpt_dag.py` | `import pydantic.v1 as pyd` → `import pydantic as pyd` | v1 shim import |
| S2b-T9 | `tests/pipelime/sequences/test_validation.py` | `import pydantic.v1 as pyd` → `import pydantic as pyd` (module + 2 local imports); `@pyd.validator("cfg")` → `@pyd.field_validator("cfg")` + `@classmethod`; `extra=pyd.Extra.ignore/forbid` → `extra="ignore"/"forbid"` | v1 shim import, v1 `validator`, v1 `Extra` enum |
| S2b-T9 | `tests/pipelime/sequences/test_sample.py` | `from pydantic.v1 import BaseConfig, Extra, create_model` → `from pydantic import ConfigDict, create_model`; `class SampleConfig(BaseConfig)` / `SampleConfig2` → `ConfigDict(...)` literals | v1 `BaseConfig`/`Extra` (v2 `create_model(__config__=ConfigDict)`) |
| S2b-T9 | `tests/pipelime/sequences/test_operations.py` | `from pydantic.v1 import ValidationError` → `from pydantic import ValidationError` | v1 shim import |
| S2b-T9 | `tests/pipelime/sequences/test_samples_sequences.py` | `X.__config__.title` → `X.model_config.get("title")` (3 places) | v1 `__config__` |
| S2b-T9 | `tests/pipelime/stages/test_base_stages.py` | `from pydantic.v1 import BaseModel` → `from pydantic import BaseModel` | v1 shim import |
| S2b-T9 | `tests/pipelime/stages/test_entities.py` | `from pydantic.v1 import BaseModel, ValidationError, parse_obj_as` → `from pydantic import BaseModel, TypeAdapter, ValidationError`; `parse_obj_as(StageEntity, x)` → `TypeAdapter(StageEntity).validate_python(x)`; `X.__config__.extra` → `X.model_config.get("extra")` (3 places) | v1 shim import, `parse_obj_as`, `__config__` |
| S2b-T9 | `tests/pipelime/stages/test_item_info.py` | `from pydantic.v1 import parse_obj_as` → `from pydantic import TypeAdapter`; `parse_obj_as(ItemInfo, v)` → `TypeAdapter(ItemInfo).validate_python(v)` | v1 `parse_obj_as` |
| S2b-T9 | `tests/pipelime/piper/test_command_decorator.py` | `from pydantic.v1 import Field` → `from pipelime.piper import Field` | v1 shim import |
| S2b-T9 | `tests/pipelime/piper/test_dag.py` | `from pydantic.v1 import Field, ValidationError` → `from pydantic import ValidationError` + `from pipelime.piper import Field` | v1 shim import; the fields carry `piper_port=` |
| S2b-T9 | `tests/pipelime/piper/test_commands.py` | `from pydantic.v1 import BaseModel` → `from pydantic import BaseModel, RootModel`; `class _DotsTestData(BaseModel): __root__: ...` → `class _DotsTestData(RootModel[t.Sequence[_DotOpts]])`; `.parse_obj(` → `.model_validate(`; `.__root__` → `.root` | v1 shim import, v1 custom-root declaration, `parse_obj` |
| S2b-T9 | `tests/pipelime/piper/test_dag_parsers.py` | `from pydantic.v1 import BaseModel` → `from pydantic import BaseModel`; `DAGModel.parse_obj(` → `DAGModel.model_validate(` (2 places) | v1 shim import, `parse_obj` |
| S2b-T9 | `tests/pipelime/commands/test_pipe.py` | `from pydantic.v1 import ValidationError` → `from pydantic import ValidationError`; `PipeCommand.parse_obj(` → `PipeCommand.model_validate(` (2 places) | v1 shim import, `parse_obj` |
| S2b-T9 | `tests/pipelime/commands/test_split.py` | `from pydantic.v1 import ValidationError, parse_obj_as` → `from pydantic import TypeAdapter, ValidationError`; `parse_obj_as(T, v)` → `TypeAdapter(T).validate_python(v)` (6 places); `X.parse_obj(` → `X.model_validate(` (3 places) | v1 shim import, `parse_obj_as`, `parse_obj` |
| S2b-T9 | `tests/pipelime/commands/test_interfaces.py` | `from pydantic.v1 import ValidationError, create_model` → `from pydantic import ...`; `model_cls.__fields__[f].field_info.description` → `model_cls.model_fields[f].description`; `.field_info.extra` → `model_cls.model_fields[f].json_schema_extra or {}`; `model_cls.parse_obj(` → `model_cls.model_validate(`; `for k in model_cls.__fields__` → `model_fields`; `v.get_default()` → `v.get_default(call_default_factory=True)` | v1 shim import, `__fields__`/`field_info`, `parse_obj` |
| S2b-T9 | `tests/pipelime/test_pydantic_contract.py::TestSequences::test_to_pipe_roundtrip` | expected key `"contract_pipe"` → `f"{__name__}:contract_pipe"` | pin corrected to the pipelime fact: `_add_operator_path` (byte-identical to 2.x) serializes operators defined outside `pipelime` with their module path; the pin was never exercised on v1 (the test is `xfail(V1, strict=True)` because of the `to_pipe` str-recursion bug) |
| S2b-T9 | `tests/pipelime/test_pydantic_contract.py::TestModernTypeHints::test_validation` | `c.f == 5` → `c.f == (5 if V1 else "5")` | pin corrected to the design decision: v2 smart unions keep the exact type for `int \| str` given `"5"` (spec risks section: accepted change, called out in the migration guide); v1 resolved left-to-right |
| S3-T2 | `tests/pipelime/cli/test_tui.py` | `from pydantic.v1 import BaseModel, Field` → `from pydantic import BaseModel` + `from pipelime.piper import Field`; added `from pipelime.utils.pydantic_compat import get_field, iter_fields`; `X.__fields__["n"]` → `get_field(X, "n")` (incl. inside `init_tui_field(...)` calls); `.field_info.description` → `.description`; `X.__fields__["grabber"].get_default()` → `get_field(X, "grabber").default`; `fields = ModelWithComplexFieldsTypes.__fields__` → `{f.name: f for f in iter_fields(ModelWithComplexFieldsTypes)}` | v1 shim import, v1 `__fields__`/`ModelField` introspection (the TUI helpers now take `FieldView`s) |
| S3-T3 | `tests/pipelime/cli/test_base.py` | `from pydantic.v1 import Field` → `from pipelime.piper import Field` (merged into the existing `from pipelime.piper import PipelimeCommand` line); both local `from pydantic.v1 import ValidationError` → `from pydantic import ValidationError` | v1 shim import (the command raises v2 `ValidationError`s) |
